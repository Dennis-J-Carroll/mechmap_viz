# Railway deployment environment variables
# Set these in Railway dashboard → Variables tab

# SQLite database — mount a Railway Volume at /data first
DATABASE_URL="file:/data/mechmap.db"

# Railway sets PORT automatically — do not override
# NODE_ENV is set in the Dockerfile
# Do NOT set HOSTNAME — it conflicts with Docker's reserved variables
